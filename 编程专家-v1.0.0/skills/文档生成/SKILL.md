---
name: 文档生成
description: >
  根据代码生成技术文档，包括函数文档、README、API文档和架构说明，支持多种文档格式。
  触发词："写文档"、"生成文档"、"写README"、"文档生成"、"技术文档"、
  "写注释"、"生成API文档"、"写说明"、"documentation"、
  "帮我写文档"、"代码文档"、"项目文档"。
argument-hint: "粘贴代码或描述需要生成的文档类型"
name_en: "doc-generation"
description_en: >
  Generate technical documentation from code, including function docs, README,
  API docs, and architecture descriptions. Supports multiple doc formats.
  Trigger phrases: "write docs", "generate documentation", "write README",
  "technical docs", "code documentation", "project docs", "generate API docs".
---

# /文档生成 -- 智能文档生成

> 如遇到不熟悉的占位符，参见 [CONNECTORS.md](../../CONNECTORS.md)。

**独立能力（无需连接器）**

- 从代码提取函数/类文档
- 生成项目README
- 生成API接口文档
- 生成架构说明和流程图（文本形式）

**增强能力（连接器加持）**

- ~~Filesystem → 读取整个项目结构，生成完整的项目文档

## 连接器（可选增强）

| 连接器 | 增强能力 |
|--------|---------|
| **~~Filesystem** | 读取项目目录结构和所有源码，生成完整项目文档 |

> 没有连接器也完全可以使用——基于用户提供的代码片段生成。

## 调用方式

```
/文档生成 <代码或文档类型>
```

生成以下文档：@$1

## 输入要求

1. **代码/项目**（必填）：需要生成文档的代码或项目描述
2. **文档类型**（必填）：函数文档/README/API文档/架构文档
3. **输出格式**（可选）：Markdown/HTML/reStructuredText，默认Markdown
4. **目标读者**（可选）：开发者/用户/运维，默认开发者
5. **语言**（可选）：中文/英文，默认中文

## 执行流程

### 第一步：Spec一致性检查（Harness增强）

编写文档前检查与spec的一致性：
- **如有spec**：确保文档描述与spec中的scenario一致
- **发现不一致**：标注文档与spec的偏差，建议同步更新
- **无spec**：文档中标注"基于当前实现，建议补充spec"

```
✓ Spec一致性检查:
- 文档描述 vs Spec: [一致/不一致]
- 需要同步更新: [是/否]
```

### 第二步：内容分析

- 分析目标受众（开发者/用户/维护者）
- 确定文档类型和深度
- 识别关键信息点

### 第三步：结构设计

- 设计文档大纲和章节
- 确定示例和图示
- 规划交叉引用

### 第四步：内容编写

- 编写清晰简洁的说明
- 添加代码示例和截图
- 包含使用场景和最佳实践
- **引用Spec**：在相关章节引用spec中的scenario作为行为依据

### 第五步：质量检查

- 检查准确性和完整性
- 验证代码示例可运行
- 确认格式一致性
- **Spec同步**：确认文档与spec无冲突

## 输出格式

### 函数文档示例

```markdown
## [函数名]

**功能**：[一句话说明]

**签名**：
```[语言]
[函数签名]
```

**参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| [参数] | [类型] | [是/否] | [说明] |

**返回值**：
| 类型 | 说明 |
|------|------|
| [类型] | [说明] |

**异常**：
| 异常 | 说明 |
|------|------|
| [异常] | [触发条件] |

**示例**：
```[语言]
[使用示例]
```
```

### README示例

```markdown
# [项目名]

[项目简介]

## 功能特性

- [特性1]
- [特性2]

## 安装

```bash
[安装命令]
```

## 使用

```[语言]
[使用示例]
```

## API文档

参见 [API文档](./docs/API.md)

## 贡献

[贡献指南]
```

## 质量标准

- 文档必须准确反映代码功能，不能虚构不存在的功能
- 参数说明必须包含类型、必填性和取值范围
- 示例代码必须可直接运行，不能是伪代码
- README必须包含安装和使用说明，不能只有功能列表
- API文档必须包含错误码和异常情况
- 不得遗漏关键信息（如必要的配置项、环境要求）

## 关联Skill

- **代码审查** — 生成前可用 `/代码审查` 确保代码逻辑正确
- **API设计** — API设计完成后可用 `/文档生成` 生成接口文档
- **代码生成** — 生成代码后可用 `/文档生成` 生成配套文档
